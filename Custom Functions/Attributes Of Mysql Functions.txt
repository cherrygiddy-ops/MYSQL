1.DETERMINISTIC->Means gives a functions the same set of values it will always return the same output eg calculating the 10% of tax on each order item
2.READS SQL DATA ->Function reads SQL data
3.MODIFIES SQL DATA ->Update,delete or insert statements in a functions